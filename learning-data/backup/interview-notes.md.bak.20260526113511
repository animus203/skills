# 服务端开发与面试知识手册 — 面试速答框架

> 随学随记，每个概念学完后立即记录，不遗漏。
> 格式：过程 → 原理 → 坑点 → 一句话兜底

---

## 基础篇

### TCP三次握手

**过程：**
- 客户端发 SYN，进入 `SYN_SENT`
- 服务端回 SYN-ACK，进入 `SYN_RCVD`
- 客户端回 ACK，双方进入 `ESTABLISHED`

**核心原理：**
- 防止历史重复连接：第三次握手让客户端有机会拒绝过时的延迟 SYN
- 同步双方初始序列号（ISN）：两次握手只能确认发起方的 ISN，无法确认接收方的 ISN

**容易踩的坑：**
- 问"为什么不是两次"时，不能只答"防止历史连接"，必须同时答"序列号同步"
- 问"为什么不是四次"时，答"第二次握手把 SYN 和 ACK 合并了，省一个 RTT"

**面试一句话：**
三次握手是为了防止历史重复连接并同步双方初始序列号，两次做不到双向确认，四次浪费 RTT。

---

### TCP四次挥手

**过程：**
- 主动方发 FIN，进入 `FIN_WAIT_1`
- 被动方回 ACK，进入 `CLOSE_WAIT`
- 被动方发 FIN，进入 `LAST_ACK`
- 主动方回 ACK，进入 `TIME_WAIT`（持续 2MSL 后关闭）

**核心原理：**
- 需要四次是因为 TCP 全双工，被动方收到 FIN 后可能还有数据要发，ACK 和 FIN 不能合并
- 如果被动方刚好没数据了，ACK 和 FIN 可以合并，此时会退化成三次

**TIME-WAIT 的作用：**
- 保证最后一个 ACK 丢了能被重传（被动方重传 FIN，主动方还在 TIME_WAIT 能回 ACK）
- 让网络中旧报文彻底消失，避免影响下一个同四元组的新连接

**容易踩的坑：**
- TIME_WAIT 过多 = 主动关闭方过多。服务端多说明服务端在主动关，客户端多说明客户端在主动关
- 解决思路：负载均衡分层、端口复用（SO_REUSEADDR）、调小 2MSL（谨慎）

**面试一句话：**
四次挥手是因为被动关闭方可能还有数据要发，ACK 和 FIN 不能合并。TIME_WAIT 持续 2MSL，确保最后一个 ACK 不丢、旧报文不干扰新连接。

---

### OSI 与 TCP/IP 模型

**过程/结构：**
- OSI 七层（从下往上）：物理层、数据链路层、网络层、传输层、会话层、表示层、应用层
- TCP/IP 五层：把 OSI 的上三层合并为应用层

**核心原理：**
- OSI 是理论参考模型，TCP/IP 是工业实现
- 实际网络协议栈按 TCP/IP 五层实现

**容易踩的坑：**
- 交换机工作在二层（数据链路层），路由器工作在三层（网络层）
- OSI 七层别记反顺序，面试从下往上说更顺

**面试一句话：**
OSI 七层是理论参考模型，TCP/IP 五层是工业实现，区别在于 TCP/IP 把应用、表示、会话合并成了应用层。交换机工作在二层，路由器工作在三层。

---

### TCP 与 UDP 区别及场景

**过程/结构：**
- TCP：面向连接、可靠、字节流，首部 20-60 字节
- UDP：无连接、不可靠、数据报，首部 8 字节

**核心原理：**
- TCP 通过三次握手、确认重传、流量控制、拥塞控制保证可靠，代价是速度和 overhead
- UDP 不做任何可靠性保证，把控制交给应用层，所以快、开销小

**容易踩的坑：**
- DNS 查询用 UDP，但区域传输和大响应用 TCP
- HTTP/3 用 QUIC，QUIC 基于 UDP 但在应用层自实现可靠性
- UDP 有边界，不会粘包；TCP 是字节流，需要应用层自己处理边界

**面试一句话：**
TCP 面向连接、可靠、字节流，适合文件传输和 HTTP；UDP 无连接、不可靠、数据报，适合音视频和 DNS。TCP 首部 20-60 字节，UDP 只有 8 字节。

---

### TCP 滑动窗口与拥塞控制

**过程/结构：**
- TCP 可靠传输靠：分割、编号、校验和、流量控制、拥塞控制、超时重传
- 发送窗口 = min(接收窗口 rwnd, 拥塞窗口 cwnd)
- 拥塞控制两个阶段：慢开始（指数增长）→ 拥塞避免（线性增长）

**核心原理：**
- 流量控制是端到端的，防止发送方压垮接收方（靠 rwnd）
- 拥塞控制是全局网络的，防止发送方压垮整个网络（靠 cwnd）
- 超时拥塞：ssthresh 减半，cwnd 回 1，重新慢开始
- 3 个重复 ACK：快速重传 + 快速恢复，ssthresh 减半但 cwnd 不回到 1

**容易踩的坑：**
- 流量控制和拥塞控制的对象不同：一个是接收方缓存，一个是网络链路
- 快速重传和超时重传的处理不一样，不要混为一谈
- 窗口大小是 16bit，最大 65535 字节，但通过窗口缩放选项可以更大

**面试一句话：**
TCP 靠滑动窗口实现流水线传输，靠接收窗口做流量控制、靠拥塞窗口做拥塞控制。拥塞控制分慢开始（指数增）和拥塞避免（线性增），超时则 ssthresh 减半、cwnd 回 1，3 个重复 ACK 则快速重传不回到慢开始。

---

### TCP 粘包原因和解决方法

**过程/结构：**
- TCP 是字节流，无消息边界，导致一次发送不等于一次接收
- 发送方原因：Nagle 算法缓存小数据包，合并发送
- 接收方原因：接收缓冲区读取速度慢于接收速度

**核心原理：**
- 粘包不是 TCP 的 bug，是字节流协议的固有特性
- UDP 是数据报，有边界，不会粘包

**容易踩的坑：**
- 答 TCP 会丢包所以粘包是错的。TCP 不丢包，但会合并
- 实时场景（游戏、IM）通常关闭 Nagle（TCP_NODELAY）
- 最通用的解法是 4 字节包头存长度 + 变长包体，不是定长包

**面试一句话：**
TCP 是字节流无边界，Nagle 算法和接收缓存速度差导致粘包。解决核心是给消息加边界，最常用的是 4 字节包头存长度 + 变长包体。

---

### TCP 报文格式

**过程/结构：**
- TCP 首部固定 20 字节，最多 60 字节（首部长度 4 bit，最大 15 × 4 = 60）
- 关键字段：序号（ISN）、确认序号、标志位（SYN/ACK/FIN/RST）、窗口大小、检验和

**核心原理：**
- 序号是字节级的，SYN 消耗一个序号，所以第一个数据字节 = ISN + 1
- ACK 总是免费的，连接建立后 ACK 标志和确认序号字段一直有效
- 窗口大小 16 bit 最大 65535，但窗口缩放选项可以突破

**容易踩的坑：**
- TCP 检验和覆盖首部和数据，不是只覆盖首部
- 首部长度单位是 32 bit 字（4 字节），不是字节
- RST 和 FIN 区别：RST 是异常重置，FIN 是正常关闭

**面试一句话：**
TCP 首部固定 20 字节，关键字段有序号（ISN）、确认序号、6 个标志位、窗口大小（16 bit 最大 65535）。SYN 消耗一个序号，ACK 免费且连接建立后一直有效。

---

### UDP 报文格式

**过程/结构：**
- UDP 首部固定 8 字节：源端口、目的端口、长度、检验和

**核心原理：**
- 无序号、无确认、无窗口、无标志位——极简设计，速度优先
- 检验和是可选的（IPv4 可以设为 0），IPv6 强制要求

**容易踩的坑：**
- UDP 长度字段包含首部和数据
- UDP 检验和也覆盖伪首部
- 检验和错误直接丢弃，不重传不通知

**面试一句话：**
UDP 首部只有 8 字节，无序号无确认无窗口，检验和可选，错了直接丢弃。

---

### IP 报文格式

**过程/结构：**
- IP 首部固定 20 字节，最多 60 字节
- 关键字段：版本（4/6）、首部长度、总长度、标识、标志、片偏移、TTL、协议号、首部检验和、源 IP、目的 IP

**核心原理：**
- TTL（Time To Live）：每经过一个路由器减 1，到 0 丢弃，防止路由循环
- 协议号标识上层协议：6=TCP，17=UDP，1=ICMP
- 首部检验和只覆盖 IP 首部，不覆盖数据

**面试一句话：**
IP 首部固定 20 字节，关键字段有 TTL（防路由循环）、协议号（6=TCP，17=UDP）、片偏移（分片重组）、源/目的 IP。

---

### 以太网报文格式

**过程/结构：**
- 目的 MAC（6 字节）+ 源 MAC（6 字节）+ 类型（2 字节）+ 数据 + FCS（4 字节）

**核心原理：**
- MAC 地址 48 位，网卡出厂固化，全球唯一
- 类型字段标识上层协议：0x0800=IP，0x0806=ARP

**面试一句话：**
以太网帧头 14 字节（目的 MAC 6 + 源 MAC 6 + 类型 2），MAC 地址 48 位网卡出厂固化。

---

### HTTP 协议版本

**过程/结构：**
- HTTP/1.0：每个请求后断开 TCP，无状态
- HTTP/1.1：Keep-Alive 持久连接、Content-Length、管道化（但有队头阻塞）
- HTTP/2.0：二进制帧、多路复用（一个连接多个流）、头部压缩、服务器推送

**核心原理：**
- HTTP/1.1 的管道化要求响应按顺序返回，第一个慢就堵死后面所有请求
- HTTP/2 用 Stream ID 区分不同请求，乱序收发、客户端按 ID 重组，彻底解决队头阻塞
- HTTP/2 仍然基于 TCP，TCP 丢包会导致所有流等待重传（TCP 队头阻塞）

**容易踩的坑：**
- HTTP 本身永远是无状态的，Keep-Alive 是 TCP 连接复用，不是状态保持
- HTTP/2 的多路复用解决了 HTTP 队头阻塞，但没解决 TCP 队头阻塞
- HTTP/3 用 QUIC（基于 UDP）彻底解决 TCP 队头阻塞问题

**面试一句话：**
HTTP/1.0 短连接性能差，HTTP/1.1 用 Keep-Alive 持久连接但仍有队头阻塞，HTTP/2 用二进制帧和多路复用彻底解决 HTTP 队头阻塞，但仍受限于 TCP 队头阻塞，所以 HTTP/3 改用 QUIC。

---

### HTTP 与 HTTPS 区别

**过程/结构：**
- HTTP 端口 80，明文传输，不需要证书
- HTTPS 端口 443，SSL/TLS 加密，需要 CA 证书

**核心原理：**
- HTTPS = HTTP + SSL/TLS，在 HTTP 和 TCP 之间加了一层安全层
- SSL/TLS 提供三大保证：加密（防窃听）、身份认证（防中间人）、完整性校验（防篡改）

**容易踩的坑：**
- HTTPS 不是绝对安全：中间人攻击（伪造证书）、钓鱼网站（合法证书+假冒域名）、协议降级攻击（POODLE）
- HTTPS 慢不是因为 HTTP 慢，是因为 SSL 握手和加解密开销
- 自签名证书能加密但没有 CA 认证，浏览器会报警告

**面试一句话：**
HTTP 明文传输端口 80，HTTPS 在 HTTP 和 TCP 之间加了 SSL/TLS 层做加密和身份认证，端口 443，需要 CA 证书。HTTPS 不是绝对安全，仍可能被中间人攻击、钓鱼、协议降级。

---

### HTTPS 链接建立过程（SSL/TLS 握手）

**过程/结构：**
- Client Hello（TLS 版本、加密套件、随机数）
- Server Hello + 证书（Server Random + 公钥 + CA 签名）
- 客户端验证证书，生成对称密钥，用公钥加密后发给服务端
- 服务端私钥解密，用对称密钥确认
- 后续通信全用对称加密

**核心原理：**
- 非对称加密只用来安全传输对称密钥，实际数据传输用对称加密（性能考虑）
- 客户端验证证书时用内置 CA 公钥验证签名，确保证书未被篡改

**容易踩的坑：**
- 握手阶段是两次 RTT，TLS 1.3 优化为 1-RTT
- 私钥只在服务端，绝不网络传输；公钥在证书里公开
- 对称密钥是临时生成的（每个会话不同），叫 Session Key

**面试一句话：**
HTTPS 握手先用非对称加密交换对称密钥，之后用对称加密通信。服务端发证书（含公钥），客户端验证后用公钥加密对称密钥，服务端私钥解密。非对称只传密钥，对称加密传数据。

---

### 对称加密与非对称加密

**过程/结构：**
- 对称加密：双方共享同一个密钥，速度快，适合大数据量（DES、AES）
- 非对称加密：公钥/私钥成对，公钥公开私钥保密，速度慢，适合密钥交换和签名（RSA、DSA、ECDSA）

**核心原理：**
- 对称加密的瓶颈是密钥分发——如何安全地把密钥交给对方
- 非对称加密解决了密钥分发问题，但性能差，所以实际场景是'非对称传密钥，对称传数据'

**容易踩的坑：**
- RSA 加密有长度限制，不能直接加密大量数据
- AES 有多种模式（ECB、CBC、GCM），ECB 不安全
- 非对称加密的'私钥签名、公钥验证'和'公钥加密、私钥解密'是两个不同的使用场景

**面试一句话：**
对称加密速度快但密钥分发困难，非对称加密解决了密钥分发但速度慢，所以 HTTPS 用非对称加密传对称密钥，之后用对称加密传数据。

---

### GET 和 POST 请求区别

**过程/结构：**
- GET：参数在 URL，用于查询，幂等，可被缓存
- POST：参数在 Body，用于创建/提交，非幂等，默认不缓存

**核心原理：**
- 从 RFC 语义上，GET 是'读取'（安全、幂等），POST 是'创建/处理'（非幂等）
- GET 受 URL 长度限制（约 2KB），POST 无限制
- GET 能被浏览器缓存，POST 默认不能

**容易踩的坑：**
- POST 不一定比 GET 安全，明文下都能被截获，必须用 HTTPS
- GET 也可以带 body（RFC 允许），但多数服务器不支持
- PUT 是幂等的，POST 不是幂等的

**面试一句话：**
GET 参数在 URL、用于查询、幂等、可缓存；POST 参数在 Body、用于创建/提交、非幂等、默认不缓存。两者安全性差异不大，明文下都能被截获，必须用 HTTPS。

---

### HTTP 常见响应状态码

**过程/结构：**
- 1xx 信息，2xx 成功，3xx 重定向，4xx 客户端错误，5xx 服务端错误
- 常考：200/201/301/302/304/400/401/403/404/500/502/503/504

**核心原理：**
- 301 永久重定向（SEO 权重转移），302 临时重定向
- 401 是'未认证'，403 是'已认证但无权限'
- 502 是后端挂了/返回无效响应，504 是后端响应超时

**容易踩的坑：**
- 401 Unauthorized 实际意思是'未认证'（Unauthenticated），不是'未授权'
- 403 Forbidden 才是'无权限'
- 304 不是错误，是缓存命中

**面试一句话：**
状态码分 5 类：1xx 信息、2xx 成功、3xx 重定向、4xx 客户端错、5xx 服务端错。常考 200/201/301/302/401/403/404/500/502/504。

---

### 重定向和转发区别

**过程/结构：**
- 重定向：2 次请求，地址栏变，可跨站点，request 数据不共享
- 转发：1 次请求，地址栏不变，仅限当前服务器，request 数据可共享

**核心原理：**
- 重定向是客户端行为（浏览器收到 302 自动跳转）
- 转发是服务端行为（服务器内部调度）

**容易踩的坑：**
- 提交表单后防重复提交用重定向（PRG 模式），MVC 内部视图跳转用转发
- 重定向后传递数据只能用 URL 参数、Session 或 Cookie，不能用 request 属性
- 转发不能跨服务器，重定向可以

**面试一句话：**
重定向是 2 次请求、客户端跳转、地址栏变、可跨域；转发是 1 次请求、服务端内部调度、地址栏不变、不能跨域。转发能共享 request 数据，重定向不能。

---

### Cookie 和 Session 区别

**过程/结构：**
- Cookie 存在客户端，Session 存在服务端
- Session ID 通常通过 Cookie 传递，两者配合使用

**核心原理：**
- Cookie 不安全（客户端可见可改），Session 相对安全（数据在服务端）
- 但 Session ID 被盗后同样可以冒充用户，所以都要配合 HTTPS + HttpOnly + Secure

**容易踩的坑：**
- Session 不是绝对安全，Session ID 被盗 = 身份被盗
- JWT（纯 Cookie 方案）服务端无状态，但签发后无法主动失效
- Cookie 有大小限制（约 4KB），Session 数据量大的话要存 Redis

**面试一句话：**
Cookie 存在客户端、容量小、不安全；Session 存在服务端、容量大、相对安全。通常配合用：服务端存 Session，通过 Cookie 传 Session ID。JWT 是把用户信息直接放 Cookie，服务端无状态但无法主动失效。

---

### 浏览器输入 URL 后的完整过程

**过程/结构：**
- DNS 解析 → TCP 连接（+ TLS 握手）→ 发送 HTTP 请求 → 服务器处理 → 返回响应 → 浏览器解析渲染 → 连接关闭
- 渲染 5 步：解析 HTML 建 DOM → 解析 CSS 建 CSSOM → 合并 Render Tree → Layout → Paint

**核心原理：**
- `<script>` 默认阻塞解析，`async` 异步下载立即执行，`defer` 异步下载延迟到 DOM 解析完再执行
- DNS 主要用 UDP，响应超 512 字节或区域传输时用 TCP

**容易踩的坑：**
- 重定向（301/302）会让浏览器重新走 DNS → TCP → HTTP 流程
- JS 修改 DOM 会触发重排（Reflow）和重绘（Repaint）

**面试一句话：**
输入 URL 后先 DNS 解析拿到 IP，三次握手建 TCP 连接，HTTPS 还要 TLS 握手，然后发 HTTP 请求，服务器返回响应，浏览器解析 HTML/CSS 构建 DOM/CSSOM，合并 Render Tree 后布局绘制。

---

### 进程与线程的区别

**过程/结构：**
- 进程：资源分配的最小单位，有独立地址空间
- 线程：任务调度和执行的最小单位，共享进程的堆和方法区，有自己的栈和程序计数器
- 协程：用户态轻量级线程，切换开销极小

**核心原理：**
- 进程间隔离，一个进程崩了不影响其他进程
- 线程间共享内存，通信成本低，但需要处理资源竞争（锁）
- 线程切换比进程切换快（不需要切换地址空间），但仍有内核态开销；协程切换完全在用户态，更快

**容易踩的坑：**
- Java 中线程共享堆和方法区，但栈是独立的。局部变量在栈上，所以是线程安全的；实例变量在堆上，多线程访问需要同步
- 协程不是操作系统原生支持的，需要语言运行时支持（Go、Python、Kotlin），Java 原生没有协程
- 进程通信（IPC）成本高，因为需要穿越地址空间边界

**面试一句话：**
进程是资源分配的最小单位，线程是调度执行的最小单位。线程共享进程的堆和方法区，有自己的栈和程序计数器。协程是用户态轻量级线程，切换开销比线程更小。

---

### 进程间通信方式（IPC）

**过程/结构：**
- 管道（FIFO，半双工，亲缘关系用匿名，非亲缘用命名）
- 信号（简单事件通知，kill 命令）
- 消息队列（结构化消息，有类型标识）
- 共享内存（最快，直接映射同一块物理内存）
- 信号量（计数器做同步）
- 套接字（跨机器通信）

**核心原理：**
- 共享内存最快，但必须配合信号量做同步
- 管道只能传字节流，消息队列可以传结构化数据
- Socket 是唯一支持跨机器的 IPC

**容易踩的坑：**
- 管道是半双工的，双向通信需要建两个管道
- 信号量不是用来传数据的，是做同步的。共享内存 + 信号量 = 经典组合
- 面试常问最快的 IPC，答案是共享内存

**面试一句话：**
6 种方式：管道、信号、消息队列、共享内存、信号量、套接字。最快是共享内存，但必须配信号量做同步。Socket 是唯一支持跨机器的。

---

### 用户态和内核态

**过程/结构：**
- 用户态：受限，运行用户程序，只能访问自己的内存
- 内核态：最高权限，运行 OS 内核，可访问所有内存和设备
- 切换 3 种方式：系统调用（主动）、异常（被动）、设备中断（被动）

**核心原理：**
- 用户态和内核态的隔离是操作系统安全的基础
- 系统调用本质是软中断，是用户程序请求内核服务的唯一合法途径
- 上下文切换时，CPU 会保存用户态的寄存器状态到内核栈

**容易踩的坑：**
- Java 程序员不需要写系统调用，但 System.out.println()、文件 IO、网络 IO 底层都会触发系统调用
- 线程切换也需要进入内核态（调度器在内核），这就是内核态切换的开销
- 现代 CPU 有 Ring 0-3 四个特权级，Linux 只用了 Ring 0（内核态）和 Ring 3（用户态）

**面试一句话：**
用户态运行应用程序，权限受限；内核态运行 OS 内核，可访问所有资源。切换有 3 种方式：系统调用（主动）、异常（被动）、设备中断（被动）。隔离目的是保护系统安全。

---

### 内存管理（虚拟内存 + 分页）

**过程/结构：**
- 虚拟内存：每个进程独立虚拟地址空间（32 位 4GB），OS 通过页表映射到物理内存
- 分页：虚拟/物理内存切成固定大小页（通常 4KB），CPU 通过 MMU 查页表转换地址
- 缺页中断：访问的页不在物理内存，OS 从磁盘加载该页

**核心原理：**
- 虚拟内存实现进程隔离和超过物理内存的寻址能力
- 页表存储在内存中，TLB（快表）缓存常用页表项加速查询
- 缺页中断是 CPU 正常机制，不是错误

**容易踩的坑：**
- 频繁缺页会导致抖动（Thrashing），系统性能暴跌
- Swap 分区是磁盘，访问速度比内存慢万倍，大量 Swap 意味着系统快挂了

**面试一句话：**
虚拟内存让每个进程有独立地址空间，OS 通过页表把虚拟页映射到物理页。访问不在内存的页触发缺页中断，从磁盘加载。TLB 缓存加速页表查询。

---

### 页面置换算法

**过程/结构：**
- FIFO：先进先出，淘汰最早进入的页
- LRU：最近最少使用，淘汰最久没被访问的页
- Clock（时钟）：近似 LRU，用访问位循环扫描，1 置 0 再给机会，0 淘汰

**核心原理：**
- 局部性原理：刚访问的页很可能再次被访问，所以 LRU 效果通常最好
- FIFO 有 Belady 异常：页框增加，缺页反而变多

**容易踩的坑：**
- LRU 实现复杂（需要双向链表 + HashMap），Clock 是工程常用折中方案
- 没有算法在所有场景下都最优，要看访问序列特征

**面试一句话：**
物理内存满时换出旧页。FIFO 简单但有 Belady 异常；LRU 最优但实现复杂；Clock 用访问位近似 LRU，兼顾效果和成本。

---

### 死锁

**过程/结构：**
- 四个必要条件：互斥、占有且等待、不可抢占、循环等待
- 预防：破坏四条件之一（一次性申请所有资源、按序申请、允许抢占）
- 避免：银行家算法，运行时检查安全序列
- 检测与恢复：资源分配图检测，终止进程或剥夺资源

**核心原理：**
- 死锁本质是资源竞争导致的循环等待
- 银行家算法的核心是找出安全序列，确保系统不会进入不安全状态

**容易踩的坑：**
- 数据库事务中也有死锁（如 MySQL 的 gap lock 循环等待），解决方案是设置超时或按主键顺序加锁
- 预防死锁的代价通常是降低并发度（如一次性申请所有资源）

**面试一句话：**
死锁是进程循环等待资源导致永久阻塞，四个必要条件缺一不可。预防是破坏条件（如按序申请），避免是用银行家算法检查安全序列。

---

### 进程空间（栈区、堆区、静态区、代码区）

**过程/结构：**
- 栈区：局部变量、函数参数、返回地址，编译器自动管理，先进后出
- 堆区：动态分配（new/malloc），程序员或 GC 管理
- 静态区：全局变量、静态变量，程序启动时分配，结束时释放
- 代码区：可执行代码、常量，只读

**核心原理：**
- 进程空间是 OS 给每个进程分配的虚拟地址空间（32 位系统 4GB）
- 分区目的是隔离和管理：栈区保证函数调用安全，堆区支持动态内存，静态区保证全局数据持久，代码区保护指令不被篡改

**容易踩的坑：**
- `char *p = "hello"` 中字符串在常量区（只读），修改会段错误；`char arr[] = "hello"` 中数组在栈区，可以修改
- Java 的栈对应 JVM 栈，操作系统的栈对应函数调用栈，概念相通但层面不同
- 堆内存泄漏：C/C++ 忘记 free 会泄漏；Java 有 GC 但仍可能因为强引用导致对象无法回收

**面试一句话：**
进程地址空间分为栈区（局部变量，自动管理）、堆区（动态分配，手动/GC管理）、静态区（全局变量，程序生命周期）、代码区（可执行代码，只读）。`char *p` 指向常量区只读，`char arr[]` 在栈区可读写。

---

### 面向对象三大特征

**过程/结构：**
- 封装：隐藏内部实现，暴露公共接口（private 字段 + getter/setter）
- 继承：子类继承父类属性和方法，代码复用，Java 单继承
- 多态：同一接口不同实现，提高扩展性

**核心原理：**
- 封装降低耦合，继承复用代码，多态实现运行时扩展
- 多态依赖运行时绑定（动态分派），是面向对象设计的核心优势

**容易踩的坑：**
- 过度继承会导致类层次过深，优先考虑组合优于继承
- 封装不是把所有字段都 private 然后无脑加 getter/setter，要根据业务设计暴露粒度

**面试一句话：**
封装是隐藏实现暴露接口，继承是代码复用（Java 单继承），多态是同一接口不同实现。

---

### Java vs C++ 区别

**过程/结构：**
- Java 无指针只有引用，C++ 有指针
- Java GC 自动回收，C++ 手动 new/delete
- Java 单继承多实现，C++ 多继承
- Java 编译成字节码跨平台（JVM），C++ 编译成机器码平台相关

**核心原理：**
- Java 通过 JVM 屏蔽平台差异，牺牲部分性能换取可移植性
- C++ 直接操作内存和硬件，性能更高但开发复杂度和出错率也高

**容易踩的坑：**
- Java 的引用和 C++ 的引用概念不同，Java 引用更接近 C++ 的指针（可以重新赋值）
- Java 没有析构函数，不要依赖 finalize() 做资源释放，用 try-with-resources

**面试一句话：**
Java 无指针、GC 自动回收、单继承多实现、跨平台（JVM）；C++ 有指针、手动管理内存、多继承、编译成机器码。

---

### 多态（编译时 vs 运行时）

**过程/结构：**
- 编译时多态（重载 Overload）：同类中方法名相同，参数列表不同，编译期确定
- 运行时多态（重写 Override）：子类重写父类方法，运行时根据实际对象类型调用
- 运行时多态三要素：继承 + 重写 + 父类引用指向子类对象

**核心原理：**
- 重载是静态绑定（编译时），重写是动态绑定（运行时）
- JVM 通过虚方法表（vtable）实现运行时多态

**容易踩的坑：**
- static 方法不能被重写，父类引用调用 static 方法总是调用父类版本（编译时绑定）
- private 方法也不能被重写，子类同名方法只是新方法

**面试一句话：**
编译时多态是重载（参数不同，静态绑定），运行时多态是重写（子类覆盖父类，动态绑定），需要继承 + 重写 + 父类引用指向子类对象。

---

### static / final / abstract

**过程/结构：**
- static：类级别，所有实例共享，静态方法不能访问非静态成员（无 this）
- final：类不可继承、方法不可重写、变量引用不可变（对象内容可变）
- abstract：抽象类不能实例化，可以有抽象方法和非抽象方法

**核心原理：**
- static 属于类，不依赖对象生命周期
- final 提供不变性保证，是线程安全的基础之一
- abstract 定义模板，强制子类实现特定行为

**容易踩的坑：**
- final 变量引用不可变，但如果是引用类型，对象内部状态可以变（如 final StringBuilder 可以 append）
- static 方法里不能用 this 或 super，因为static不绑定实例

**面试一句话：**
static 是类级别共享；final 管不可变（类不可继承、方法不可重写、变量引用不可变）；abstract 是不能实例化的模板。

---

### 抽象类 vs 接口

**过程/结构：**
- 抽象类：可以有成员变量、构造方法、非抽象方法；单继承（is-a）
- 接口：JDK8 前只能有常量和抽象方法；JDK8 支持 default/static 方法；JDK9 支持 private 方法；多实现（like-a）

**核心原理：**
- 抽象类是对事物的抽象（模板），接口是对行为的抽象（契约）
- 设计原则：优先用接口定义能力，用抽象类提供通用实现

**容易踩的坑：**
- 接口 default 方法冲突：一个类实现多个接口，如果两个接口有同名 default 方法，必须重写解决
- 抽象类可以有构造方法，但接口没有（JDK8 前）

**面试一句话：**
抽象类是模板（is-a，单继承，可以有成员变量和构造方法），接口是契约（like-a，多实现，JDK8 后支持 default 方法）。

---

### 泛型

**过程/结构：**
- 类型参数化，编译时类型检查，避免强制类型转换
- 类型擦除：编译后泛型信息被擦除，运行时 List<String> 和 List<Integer> 是同一个类型
- 通配符：? extends T（上界，Producer，只能读）、? super T（下界，Consumer，只能写）

**核心原理：**
- PECS 原则：Producer Extends, Consumer Super
- 泛型擦除是为了兼容 JDK5 之前的字节码

**容易踩的坑：**
- 不能创建泛型数组（new T[10] 非法）
- static 方法不能用泛型类型参数（因为类型擦除后不知道 T 是什么）
- 反射可以获取泛型信息（ParameterizedType），但运行时无法直接用 instanceof 判断泛型类型

**面试一句话：**
Java 泛型通过类型擦除实现编译期检查，运行时无泛型信息。PECS 原则：读用 extends，写用 super。

---

### 反射

**过程/结构：**
- 运行时动态获取类信息（字段、方法、构造器）并调用
- 常用 API：Class.forName、getMethod、getField、Method.invoke
- setAccessible(true) 可以突破封装访问 private 成员

**核心原理：**
- 反射绕过编译期检查，直接操作运行时元数据
- JVM 通过 Method 对象的 native 调用实现反射执行

**容易踩的坑：**
- 性能差：反射调用比直接调用慢几十倍（JVM 难以内联优化）
- 破坏封装：访问私有成员，违反面向对象设计
- 编译期无法检查：类型错误延迟到运行时暴露
- 应用场景：Spring 依赖注入、MyBatis ORM、动态代理、单元测试

**面试一句话：**
反射是运行时动态获取类信息并调用的机制，应用广泛（Spring、ORM），但性能差、破坏封装、编译期不检查类型，生产环境慎用。

---

### 异常体系

**过程/结构：**
- Error：严重错误，不可恢复（OutOfMemoryError、StackOverflowError）
- Exception → Checked（编译期必须处理，如 IOException）和 Unchecked（RuntimeException，如 NPE）
- finally：几乎一定执行，用于释放资源

**核心原理：**
- Checked 代表外部不可控因素（IO、网络），Unchecked 代表编程错误
- finally 块通过 JVM 的异常处理机制保证执行（除非 System.exit 或 JVM 崩溃）

**容易踩的坑：**
- finally 里如果有 return，会覆盖 try 里的 return
- 不要在 finally 里抛出新的异常，会覆盖 try 中的原始异常信息
- 不要捕获 Throwable 或 Exception 做笼统处理，会吞掉 Error 和 RuntimeException

**面试一句话：**
Error 是不可恢复的严重错误；Exception 分 Checked（编译期必须处理，如 IO）和 Unchecked（RuntimeException，如 NPE）。finally 几乎必执行，但里面 return 会覆盖 try 的 return。

---

### 序列化

**过程/结构：**
- 实现 Serializable 接口（标记接口，无方法），通过 ObjectOutputStream 写对象，ObjectInputStream 读对象
- serialVersionUID 用于版本校验，不声明则编译器自动生成，类结构变化后反序列化会抛 InvalidClassException
- transient 修饰的字段不参与序列化

**核心原理：**
- 序列化把对象图转成字节流，反序列化时通过反射重建对象
- 自定义类作为 key 必须同时重写 equals() 和 hashCode()

**容易踩的坑：**
- 反序列化可以执行任意代码，不可信数据不要直接反序列化（常见的攻击入口）
- 单例类如果实现 Serializable，反序列化会破坏单例，需要加 readResolve() 方法
- 生产环境多用 JSON/Protobuf 替代 Java 原生序列化

**面试一句话：**
序列化把对象转成字节流，实现 Serializable，transient 字段不参与，serialVersionUID 用于版本校验。反序列化有安全风险，生产环境多用 JSON/Protobuf。

---

### String / StringBuilder / StringBuffer

**过程/结构：**
- String：不可变（底层 final byte[] + coder），线程安全（不可变天然安全）
- StringBuilder：可变，无锁，性能最好
- StringBuffer：可变，方法加 synchronized，线程安全但性能差

**核心原理：**
- JDK9 之后 String 底层从 char[] 改为 byte[] + coder（Latin-1 用 1 字节，UTF-16 用 2 字节），节省内存
- String 常量池在 JDK7 后从方法区移到堆区
- "+" 拼接编译期会优化为 StringBuilder，但循环中每次都会创建新 StringBuilder

**容易踩的坑：**
- `new String("hello")` 会创建两个对象（堆上 + 常量池），`"hello"` 只在常量池创建一个
- `String.intern()` 把字符串放入常量池，如果常量池已有则返回已有引用
- 不要在循环里用 "+" 拼接字符串

**面试一句话：**
String 不可变（final 数组），适合少量拼接；StringBuilder 可变无锁，性能最好，适合单线程循环拼接；StringBuffer 可变有 synchronized 锁，适合多线程。JDK9 后 String 底层改 byte[] 省内存。

---

### 单例模式

**过程/结构：**
- 确保一个类只有一个实例，提供全局访问点
- 饿汉式：类加载即创建，线程安全但不延迟加载
- 懒汉式 DCL：双重检查锁 + volatile，最常用
- 静态内部类：JVM 保证线程安全，延迟加载，推荐
- 枚举：最简洁，天然防反射和反序列化破坏

**核心原理：**
- DCL 需要 volatile 禁止指令重排序（new 对象分三步：分配内存、初始化、赋值引用，重排序会导致其他线程拿到半初始化对象）
- 静态内部类只有被使用时才加载，实现延迟加载

**容易踩的坑：**
- 单例可被反射破坏（setAccessible(true) + newInstance()）
- 可被反序列化破坏（需加 readResolve() 方法返回已有实例）
- 可被克隆破坏（需重写 clone() 抛异常）

**面试一句话：**
单例确保全局唯一实例。推荐静态内部类（延迟加载+线程安全）或枚举（防反射/反序列化破坏）。DCL 是懒汉式经典写法（volatile + 双重检查）。

---

### 工厂模式 vs 抽象工厂

**过程/结构：**
- 简单工厂：一个工厂类根据参数创建不同产品，新增产品需改工厂类
- 工厂方法：抽象工厂 + 具体工厂，每个产品对应一个工厂类，符合开闭原则
- 抽象工厂：创建产品族（如 Windows 风格的按钮 + 文本框）

**核心原理：**
- 工厂方法解耦了对象的创建和使用，Spring 的 BeanFactory 是典型应用
- 抽象工厂适合产品族场景，工厂方法适合单一产品扩展

**容易踩的坑：**
- 简单工厂违反开闭原则，面试时要区分清楚三种工厂的区别
- 抽象工厂和工厂方法不是替代关系，是不同场景的选择

**面试一句话：**
工厂方法是一个产品对应一个工厂（符合开闭原则），抽象工厂是创建一个产品族（如 Windows 风格的按钮+文本框）。Spring 的 BeanFactory 是工厂模式典型应用。

---

### JVM 内存划分（运行时数据区）

**过程/结构：**
- 堆（Heap）：对象实例，GC 主要区域，线程共享
- 方法区（Metaspace）：类信息、常量、静态变量，JDK8 后使用本地内存
- 虚拟机栈：线程私有，栈帧（局部变量、操作数栈、动态链接、返回地址）
- 本地方法栈：Native 方法
- 程序计数器：字节码行号指示器
- 直接内存：NIO 使用，不受 JVM 堆大小限制

**核心原理：**
- JDK8 之前叫永久代（PermGen），容易 OOM；JDK8 后改为元空间（Metaspace），用本地内存，默认无上限
- 堆和方法区线程共享，虚拟机栈/本地方法栈/程序计数器线程私有

**容易踩的坑：**
- 直接内存不受 JVM 堆 GC 管理，但分配和回收更快
- `-XX:MaxMetaspaceSize` 控制元空间上限，不设上限可能耗尽系统内存

**面试一句话：**
JVM 内存分为堆（对象实例）、方法区（类信息，JDK8 后 Metaspace 用本地内存）、虚拟机栈（线程私有，栈帧）、本地方法栈、程序计数器。堆和方法区线程共享，栈和计数器线程私有。

---

### 堆的详细划分

**过程/结构：**
- 新生代（1/3）：Eden（8/10）、Survivor0（1/10）、Survivor1（1/10）
- 老年代（2/3）：长期存活对象

**核心原理：**
- 对象先在 Eden，Minor GC 存活对象进 Survivor，年龄达 15 进老年代
- 大对象直接进入老年代，避免新生代频繁复制

**容易踩的坑：**
- 新生代比例可调（-Xmn），但通常保持默认
- 动态年龄判定：Survivor 中同年龄对象超一半空间直接进老年代

**面试一句话：**
堆分新生代（Eden + 两个 Survivor，默认 8:1:1）和老年代。新对象在 Eden，Minor GC 存活对象进 Survivor，年龄达 15 进老年代。大对象直接进老年代。

---

### JIT（即时编译）

**过程/结构：**
- 解释执行：逐条解释字节码，启动快但执行慢
- JIT 编译：热点代码编译成本地机器码，缓存到 Code Cache
- 分层编译：C1 快速编译 + C2 深度优化

**核心原理：**
- 热点探测基于方法调用计数器和回边计数器（循环次数）
- 分层编译先用 C1 快速启动，热点方法再用 C2 深度优化（逃逸分析、内联等）

**容易踩的坑：**
- JIT 编译有开销，不是所有方法都值得编译
- Code Cache 满了会导致 JIT 编译停止，影响性能

**面试一句话：**
JIT 把热点字节码编译成本地机器码缓存起来。分层编译先用 C1 快速启动，热点方法再用 C2 深度优化。热点探测基于方法调用计数器和回边计数器。

---

### 内存分配策略

**过程/结构：**
- 指针碰撞（Bump the Pointer）：堆规整时用，Serial/ParNew 使用
- 空闲列表（Free List）：堆不规整时用，CMS 使用
- TLAB：每个线程在堆上分配私有缓冲区（Eden 的 1%）

**核心原理：**
- TLAB 避免多线程竞争分配指针，提升分配效率
- 分配失败（TLAB 用完）再走堆分配

**容易踩的坑：**
- TLAB 太小会导致频繁走堆分配，太大浪费空间
- `-XX:TLABSize` 可调整，通常用默认值

**面试一句话：**
堆内存规整时用指针碰撞（Serial、ParNew），不规整时用空闲列表（CMS）。TLAB 是每个线程的私有分配缓冲区，减少多线程竞争。

---

### 对象创建过程

**过程/结构：**
1. 类加载检查
2. 堆上分配内存
3. 初始化零值
4. 设置对象头（哈希码、GC 年龄、锁状态）
5. 执行构造函数

**核心原理：**
- 对象头包含 Mark Word（GC 信息、锁状态）和 Class Pointer（指向 Class 对象）
- 分配零值保证对象字段有默认值，构造函数做真正初始化

**容易踩的坑：**
- 对象可能通过反射、克隆、反序列化创建，不走构造函数
- 逃逸分析优化后，对象可能分配在栈上（标量替换）

**面试一句话：**
new 对象先检查类是否加载，然后在堆分配内存，初始化为零值，设置对象头，最后执行构造函数。

---

### 对象引用类型

**过程/结构：**
- 强引用：永不回收（除非置 null）
- 软引用：内存不足时回收（SoftReference，适合缓存）
- 弱引用：GC 就回收（WeakReference，WeakHashMap）
- 虚引用：追踪对象回收（PhantomReference + ReferenceQueue）

**核心原理：**
- 引用强度递减：强 > 软 > 弱 > 虚
- 虚引用必须配合 ReferenceQueue，对象回收前放入队列

**容易踩的坑：**
- System.gc() 不保证立即执行，只是建议 GC
- 软引用回收时机取决于 JVM 策略，不是精确控制

**面试一句话：**
强引用永不回收；软引用内存不足回收（缓存）；弱引用 GC 就回收（WeakHashMap）；虚引用用于追踪回收（PhantomReference）。

---

### 类加载过程

**过程/结构：**
- 加载（生成 Class 对象）
- 验证（格式/字节码）
- 准备（静态变量零值）
- 解析（符号转直接引用）
- 初始化（静态变量赋值+静态块）

**核心原理：**
- 准备阶段设零值，初始化阶段才执行真正的赋值
- 双亲委派：先委托父加载器，父加载不了才自己加载

**容易踩的坑：**
- 主动使用（new、getStatic、invokeStatic）才触发加载
- 被动使用（getStatic 的 final 常量）不触发加载

**面试一句话：**
类加载五步：加载（生成 Class 对象）、验证（格式/字节码）、准备（静态变量零值）、解析（符号转直接引用）、初始化（静态变量赋值+静态块）。

---

### 双亲委派模型

**过程/结构：**
- Bootstrap → Extension → Application → Custom
- 先委托父，父加载不了才自己加载

**核心原理：**
- 安全：防止核心类被篡改
- 避免重复加载

**容易踩的坑：**
- 打破双亲委派需重写 loadClass()
- Tomcat、Spring Boot 都打破了双亲委派

**面试一句话：**
双亲委派是类加载先委托给父，父加载不了才自己加载。好处是安全（防核心类篡改）和避免重复加载。Tomcat、Spring Boot 都打破了。

---

### Tomcat 类加载器

**过程/结构：**
- Common → Catalina / Shared → WebApp（每个应用独立）
- WebApp 先加载自己的再委托父

**核心原理：**
- 打破双亲委派实现 WebApp 隔离
- 支持多版本依赖共存和 JSP 热更新

**容易踩的坑：**
- 多个 WebApp 共享的 jar 放在 Common/lib
- 每个 WebApp 的 WEB-INF/lib 独立加载

**面试一句话：**
Tomcat 打破了双亲委派，每个 WebApp 有独立的 ClassLoader 实现隔离，先加载自己的再委托父。目的是支持多版本依赖共存和 JSP 热更新。

---

### GC 算法

**过程/结构：**
- 标记-清除：标记存活，清除死亡（碎片多）
- 标记-复制：复制到新区域（无碎片，浪费空间）
- 标记-整理：向一端移动（无碎片，移动开销大）

**核心原理：**
- 新生代存活率低，用标记-复制
- 老年代存活率高，用标记-整理/清除

**容易踩的坑：**
- CMS 用标记-清除，会有碎片问题
- G1 用标记-整理，配合 Region 管理

**面试一句话：**
GC 三大算法：标记-清除（碎片多）、标记-复制（新生代用）、标记-整理（老年代用）。新生代对象存活率低用复制，老年代存活率高用整理。

---

### 对象存活判定

**过程/结构：**
- Java 用可达性分析（非引用计数法），从 GC Roots 出发搜索，不可达对象可回收
- GC Roots 包括：栈局部变量、静态属性、常量、JNI 引用

**核心原理：**
- 引用计数法无法解决循环引用问题，Java 采用可达性分析
- 对象被标记为不可达后，还要经过 finalize() 才真正可回收（但不推荐使用）

**容易踩的坑：**
- 不要依赖 finalize() 做资源释放，用 try-with-resources
- 大对象及时置 null，帮助 GC 判定不可达

**面试一句话：**
Java 用可达性分析判断对象是否存活，从 GC Roots 出发搜索，不可达的对象可回收。GC Roots 包括栈局部变量、静态属性、常量、JNI 引用。

---

### GC 类型

**过程/结构：**
- Minor GC：新生代（Eden 满时触发），快
- Major GC：老年代，慢
- Full GC：整个堆+方法区，很慢，Stop-The-World
- Mixed GC：G1 特有，新生代+部分老年代

**核心原理：**
- Minor GC 触发 Full GC 的场景：老年代空间不足，无法容纳 Minor GC 后晋升的对象
- `System.gc()` 只是建议，可通过 `-XX:+DisableExplicitGC` 禁用

**容易踩的坑：**
- 线上 Full GC 频繁是性能问题核心指标
- 避免在代码中调用 `System.gc()`

**面试一句话：**
Minor GC 清理新生代（快），Full GC 清理整个堆+方法区（很慢，Stop-The-World）。`System.gc()` 只是建议，可禁用。

---

### 垃圾收集器

**过程/结构：**
- ParNew + CMS：低延迟组合，CMS 有 4 个阶段
- Parallel + Serial Old：吞吐量优先
- G1：JDK9 默认，Region 分区+可预测停顿
- ZGC：亚毫秒停顿，着色指针+读屏障

**核心原理：**
- CMS 4 阶段：初始标记（STW）→并发标记→重新标记（STW）→并发清除
- G1 把堆分成 Region，根据回收价值决定优先清理哪些

**容易踩的坑：**
- CMS 对 CPU 敏感（需额外线程），有碎片问题，JDK9 已废弃
- ZGC 支持 TB 级堆，停顿时间 < 1ms

**面试一句话：**
常用组合：ParNew + CMS（低延迟）、Parallel + Serial Old（吞吐量）。G1 是 JDK9 默认，Region 分区+可预测停顿。ZGC 亚毫秒停顿，适合大堆。

---

### 线程状态

**过程/结构：**
- NEW：创建但未 start
- RUNNABLE：包含 OS 的 Running 和 Ready（可运行但没分配到 CPU）
- BLOCKED：等待 synchronized 锁
- WAITING：Object.wait()、Thread.join()、LockSupport.park()，无超时
- TIMED_WAITING：Thread.sleep()、wait(timeout)、join(timeout)，有超时
- TERMINATED：执行完成或异常终止

**核心原理：**
- Java 把 OS 的 Running 和 Ready 合并为 RUNNABLE
- BLOCKED 只针对 synchronized 锁，Lock 的等待是 WAITING/TIMED_WAITING

**容易踩的坑：**
- 线程调用 start() 两次会抛 IllegalThreadStateException
- 不要依赖线程优先级（Priority），调度器通常忽略它

**面试一句话：**
Java 线程 6 种状态：NEW、RUNNABLE、BLOCKED、WAITING、TIMED_WAITING、TERMINATED。BLOCKED 等 synchronized 锁，WAITING 无限等 notify，TIMED_WAITING 等超时。

---

### 线程中断机制

**过程/结构：**
- interrupt() 设置中断标志位，不会强制停止线程
- 阻塞状态（sleep/wait/join）下调用 interrupt() 会抛出 InterruptedException 并清除中断标志
- 正常运行状态下 interrupt() 只是设标志，线程需自己检查 isInterrupted() 决定是否退出

**核心原理：**
- Thread.interrupted() 检查并清除中断标志，isInterrupted() 只检查不清除
- 中断是协作机制，不是强制机制

**容易踩的坑：**
- 捕获 InterruptedException 后如果不处理，要恢复中断标志（Thread.currentThread().interrupt()），否则上层不知道被中断了
- 不要吞掉 InterruptedException（即 catch 后什么都不做）

**面试一句话：**
interrupt() 是协作式中断：阻塞时抛 InterruptedException，运行时设标志。捕获异常后要恢复中断标志或处理，不要吞掉。

---

### 为什么用 MySQL

**过程/结构：**
- 开源免费、成熟稳定、性能优秀
- InnoDB 支持事务、行锁、MVCC
- 生态完善，主从复制和分库分表成熟

**核心原理：**
- MySQL 在互联网业务中经过大规模验证，社区活跃，问题容易找到解决方案
- 存储引擎可插拔，InnoDB 是默认引擎

**容易踩的坑：**
- 不要和 PostgreSQL 搞混场景，MySQL 更适合 OLTP（事务型），PG 更适合 OLAP（分析型）
- MySQL 8.0 之前不支持窗口函数，复杂分析查询较弱

**面试一句话：**
MySQL 开源免费、成熟稳定、性能优秀，InnoDB 支持事务和行锁，生态完善，是互联网业务首选关系型数据库。

---

### ACID

**过程/结构：**
- 原子性（A）：undo log 回滚
- 一致性（C）：由 A、I、D 共同保证
- 隔离性（I）：MVCC + 锁
- 持久性（D）：redo log WAL + binlog

**核心原理：**
- redo log 是物理日志（页修改），undo log 是逻辑日志（修改前的值）
- WAL（Write-Ahead Logging）先写日志再刷盘，保证持久性且性能高

**容易踩的坑：**
- binlog 是 Server 层逻辑日志，redo log 是 InnoDB 层物理日志
- 两阶段提交保证 binlog 和 redo log 一致性

**面试一句话：**
原子性靠 undo log 回滚，持久性靠 redo log WAL 先写日志再刷盘，隔离性靠 MVCC + 锁，一致性由前三者共同保证。

---

### 事务隔离级别

**过程/结构：**
- READ UNCOMMITTED：脏读、不可重复读、幻读
- READ COMMITTED：解决脏读，有不可重复读和幻读
- REPEATABLE READ（默认）：解决脏读和不可重复读，InnoDB 通过 MVCC + 间隙锁解决幻读
- SERIALIZABLE：全部解决，性能最差

**核心原理：**
- RC 每次读生成新 ReadView，RR 事务开始时生成 ReadView
- InnoDB 默认 RR，但 RC 在并发高的场景性能更好（减少锁冲突）

**容易踩的坑：**
- 快照读（普通 SELECT）用 MVCC 避免幻读
- 当前读（SELECT ... FOR UPDATE）用间隙锁防止幻读
- Oracle 默认 RC，MySQL 默认 RR

**面试一句话：**
InnoDB 默认 RR（可重复读），通过 MVCC 解决不可重复读，通过 MVCC + 间隙锁解决幻读。RC（读已提交）是 Oracle 默认，每次读生成新 ReadView。

---

### MVCC

**过程/结构：**
- 每行数据有隐藏字段：DB_TRX_ID（事务 ID）、DB_ROLL_PTR（回滚指针）
- ReadView 包含：m_ids（未提交事务）、min_trx_id、max_trx_id、creator_trx_id
- 可见性判断：DB_TRX_ID < min_trx_id 可见，>= max_trx_id 不可见，中间看是否在 m_ids 中

**核心原理：**
- RR 级别事务开始时生成 ReadView，整个事务用同一个视图
- RC 级别每次读都生成新 ReadView

**容易踩的坑：**
- MVCC 只解决快照读的幻读，当前读需要间隙锁
- 长事务会导致 undo log 积累，影响性能

**面试一句话：**
MVCC 通过每行数据的多个版本 + ReadView 实现不加锁的读。RR 级别事务开始时生成 ReadView，RC 级别每次读都生成新 ReadView。

---

### MySQL 锁

**过程/结构：**
- 共享锁（S）：SELECT ... LOCK IN SHARE MODE，可读不可写
- 排他锁（X）：SELECT ... FOR UPDATE / DML，不可读不可写
- 意向锁（IS/IX）：表级，用于表锁和行锁冲突检测
- 间隙锁（Gap Lock）：锁住行间隙，防止幻读
- 临键锁（Next-Key Lock）：行锁 + 间隙锁，InnoDB 默认

**核心原理：**
- 意向锁不会阻塞其他意向锁，只和表锁冲突
- 死锁时 InnoDB 自动检测并回滚代价小的事务

**容易踩的坑：**
- 无索引查询会退化为表锁
- 死锁预防：按固定顺序加锁、减少事务粒度、使用索引

**面试一句话：**
InnoDB 默认临键锁（行锁 + 间隙锁）防幻读。共享锁可读不可写，排他锁不可读不可写。死锁时 InnoDB 自动检测并回滚代价小的事务。

---

### EXPLAIN 分析及优化

**过程：**
- `EXPLAIN + SQL` 查看执行计划，核心关注四个字段
- `type`：访问类型，system > const > eq_ref > ref > range > index > ALL，看到 ALL 就要优化
- `key`：实际使用的索引，NULL 表示没走索引
- `rows`：预估扫描行数，越大越危险，是加索引的直接依据
- `Extra`：Using index（覆盖索引，好）；Using filesort / temporary（要优化）

**核心原理：**
- 最左前缀：联合索引按最左字段匹配，like 'string%' 能用索引
- 索引失效：!=、IS NULL、OR、<>、NOT IN 会导致索引失效
- 覆盖索引：查询字段在索引内，避免 SELECT * 回表

**容易踩的坑：**
- LIMIT 深分页（如 100000,10）会随 offset 增大而变慢，改用子查询或 INNER JOIN 先定位 id
- COUNT(*) 全表扫描，估算场景可用 EXPLAIN 替代

**面试一句话：**
EXPLAIN 的核心价值就是把"全表扫描"变成"索引命中"。

